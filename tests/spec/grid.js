describe("A grid", function() {

  var _private = rover.grid.__private__;

  /*it("calculates size in %age for given width", function() {
    var _width = 800,
        _xAxis = 5,
        _inPixel = _width/_xAxis,
        _inPercentage = (_inPixel/_width) * 100,
        _expected = _private.calculateSize(_width, _xAxis);

    expect(_expected).toEqual(_inPercentage);
    expect(_expected).not.toEqual(_inPixel);
  });

  it("calculates size in %age for given height", function() {
    var _height = 800,
        _yAxis = 5,
        _inPixel = _height/_yAxis,
        _inPercentage = (_inPixel/_height) * 100,
        _expected = _private.calculateSize(_height, _yAxis);

    expect(_expected).toEqual(_inPercentage);
    expect(_expected).not.toEqual(_inPixel);
  });

  it("renders grid to page", function() {
    var _xAxis = 5,
        _yAxis = 6;

    spyOn(_private, 'render');//.andCallThrough();

    _private.draw(_xAxis, _yAxis);
    expect(_private.render).toHaveBeenCalled();
  });*/
});
